import ingredientList from './ingredientList.vue';
import instructionListItem from './instructionListItem.vue';
import instructionsList from './instructionsList.vue';
import nutritionTable from './nutritionTable.vue';
import preparationList from './preparationList.vue';
import preparationTimeListItem from './preparationTimeListItem.vue';

export default {
    ingredientList,
    instructionListItem,
    instructionsList,
    nutritionTable,
    preparationList,
    preparationTimeListItem,
}